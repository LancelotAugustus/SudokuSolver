# Sudoku Solver 术语表 (v0.6.1)

## 核心术语

| 术语 | 描述 |
|------|------|
| **board** | 数独棋盘对象，用于管理数独游戏的状态和操作 |
| **cell** | 棋盘上的单个格子，用于存储数字 |
| **cells** | 棋盘上所有格子的二维列表，存储棋盘的完整状态 |
| **digit** | 填入单元格的数字，取值范围通常为1到棋盘尺寸 |
| **empty_cell** | 空格，未填入数字的单元格，用0表示 |
| **puzzle** | 数独的初始局面，包含部分已填入的数字 |
| **row** | 棋盘的行（0-based索引） |
| **col** | 棋盘的列（0-based索引） |
| **size** | 棋盘的尺寸，表示每行/每列的格子数量 |
| **solution** | 数独的解，完整且符合所有规则的棋盘状态 |
| **sudoku** | 一种逻辑数字游戏，玩家需要在9×9网格中填入数字1-9，确保每行、每列和每个3×3宫都包含所有数字且不重复 |
| **cage** | 杀手数独中的笼子，由多个相邻单元格组成，笼子内数字之和必须等于指定值且不重复 |

## 算法术语

| 术语 | 描述 |
|------|------|
| **backtracking** | 回溯算法，一种通过尝试所有可能的解决方案并在遇到无效情况时回溯的求解方法 |
| **recursion** | 递归，一种函数调用自身的编程技术，常用于回溯算法 |

## 方法术语

| 术语 | 描述 |
|------|------|
| **load_puzzle** | 加载棋盘的初始局面，将外部数据转换为棋盘状态 |
| **get_digit** | 获取指定位置的数字，返回单元格的当前值 |
| **set_digit** | 在指定位置放置数字，更新单元格的状态 |
| **remove_digit** | 移除指定位置的数字，将单元格重置为空格 |
| **find_empty_cell** | 查找第一个空格，返回空格的坐标或None（无空格时） |
| **copy** | 创建对象副本，生成一个与原对象状态相同的新对象 |
| **is_valid** | 检查是否满足规则，验证当前棋盘状态是否符合所有应用的规则 |
| **validate_compatibility** | 检查兼容性，确保棋盘与规则能够正常配合工作 |
| **try_set_digit** | 尝试在指定位置放置数字，检查放置后是否符合规则并返回结果 |
| **solve** | 使用回溯算法求解数独，递归尝试所有可能的数字组合 |
| **get_solution** | 获取求解结果，返回找到的解或None（无解时） |
| **set** | 添加规则配置，如添加温度计或设置杀手数独笼子 |

## 解析相关术语

| 术语 | 描述 |
|------|------|
| **parse_compact_puzzle** | 解析紧凑型棋盘字符串，将无空格分隔的字符串转换为整数列表 |
| **parse_spaced_puzzle** | 解析空格分隔棋盘字符串，将带空格分隔的字符串转换为整数列表 |
| **parse_compact_thermometer** | 解析紧凑型温度计字符串，将无空格分隔的温度计字符串转换为坐标列表 |
| **parse_spaced_thermometer** | 解析空格分隔温度计字符串，将带空格分隔的温度计字符串转换为坐标列表 |

## 规则相关术语

| 术语 | 描述 |
|------|------|
| **Rule** | 数独规则抽象基类，定义规则的基本接口 |
| **RowRule** | 行规则，确保每行中的数字不重复 |
| **ColumnRule** | 列规则，确保每列中的数字不重复 |
| **Normal9x9BlockRule** | 9×9宫规则，确保9×9数独中每个3×3宫的数字不重复 |
| **NonConsecutiveRule** | 非连续规则，确保正交相邻的单元格不包含连续数字 |
| **ThermometerRule** | 温度计规则，确保温度计上的数字从灯泡端开始严格递增 |
| **KillerRule** | 杀手数独规则，确保笼子内数字之和等于目标和且不重复 |
| **thermometer** | 温度计坐标列表，定义温度计的形状和方向 |
| **cage_sums** | 杀手数独中笼子内数字的目标和值列表 |

## 变量和属性术语

| 术语 | 描述 |
|------|------|
| **steps** | 求解步数，记录回溯算法尝试的步数 |
| **progress_bar** | 进度条，用于可视化求解过程的进度，代码中常缩写为`pbar` |
| **rule_name** | 规则名称，标识规则的唯一名称 |
| **thermometers** | 温度计列表，存储所有温度计的坐标信息 |
| **cages** | 杀手数独中的笼子列表，存储所有笼子的坐标信息 |
| **cage_sums** | 笼子目标和列表，与cages列表一一对应 |

## 命名约定指南

### 1. 类命名
- **样式**: PascalCase
- **示例**: `Board`, `Solver`, `RowRule`
- **规则**: 使用描述性名称，清晰指示类的用途

### 2. 方法/函数命名
- **样式**: snake_case
- **示例**: `load_puzzle`, `get_digit`, `is_valid`
- **规则**: 使用动词开头，描述清晰的动作

### 3. 变量命名
- **样式**: snake_case
- **示例**: `board`, `puzzle_data`, `thermometers`
- **规则**: 描述性名称，避免不必要的缩写
- **例外**: 上下文明确的缩写如`pbar`（进度条）可接受

### 4. 私有成员
- **样式**: _snake_case（前导下划线）
- **示例**: `_cells`, `_thermometers`
- **规则**: 用于内部状态和辅助方法

### 5. 常量
- **样式**: UPPER_SNAKE_CASE
- **示例**: `MAX_SIZE`, `EMPTY_CELL_VALUE`
- **规则**: 用于不可变值

## 术语使用规范

- **单元格**：统一使用 `cell` 和 `cells`
- **数字**：统一使用 `digit`
- **棋盘初始局面**：统一使用 `puzzle`
- **放置操作**：统一使用 `set` 相关术语
- **检查操作**：统一使用 `is_valid` 和 `validate` 相关术语
- **进度条**：代码中可使用缩写 `pbar`，文档中使用完整术语 `progress_bar`